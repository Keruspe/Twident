namespace PinoEnums {

	public enum StreamEnum {
		HOME,
		MENTIONS,
		PUBLIC,
		FAVORITES,
		DIRECT,
		SEARCH,
		GROUP
	}

	public enum MenuItems {
		SETTINGS,
		REFRESH,
		STOP,
		REMOVE,
		MORE
	}
	
	public enum StatusMenuItems {
		FAVORITE,
		RETWEET,
		REMOVE,
		REPLY
	}
	
	public enum StreamStatus {
		READY,
		UPDATING,
		PARSING,
		ERROR
	}
}
